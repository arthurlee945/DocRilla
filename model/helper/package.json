{
  "name": "helper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "gen": "rm -rf migrations && prisma migrate dev --skip-generate --name helper-file --create-only"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^5.11.0"
  }
}
